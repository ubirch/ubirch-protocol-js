const nacl = require('tweetnacl');
const util = require('tweetnacl-util');

const signMessage = function (uuid, type, payload) {
    const message = '';
    return message;
}

module.exports = {
    signMessage: signMessage
}
