
## Run it

1. Install node & yarn (on a Mac).
	`brew install node`
	`brew install yarn`

2. Install packages listed in package.json
	`yarn install`

## Test it

	`yarn test`

## Build it

	`yarn build`
